{
  "name": "bicameral-dashboard-v2",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Bicameral Dashboard v2 — Vite + TypeScript + Preact navigation shell.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/copy-artifact.mjs",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "preact": "10.24.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "2.9.1",
    "@testing-library/preact": "3.2.4",
    "happy-dom": "15.11.6",
    "typescript": "5.6.3",
    "vite": "5.4.11",
    "vite-plugin-singlefile": "2.0.3",
    "vitest": "2.1.5"
  }
}
