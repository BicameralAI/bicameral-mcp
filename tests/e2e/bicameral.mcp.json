{
  "mcpServers": {
    "bicameral": {
      "command": "bicameral-mcp",
      "args": [],
      "env": {
        "SURREAL_URL": "surrealkv://${LEDGER_DIR}",
        "REPO_PATH": "${DESKTOP_REPO_PATH}"
      }
    }
  }
}
